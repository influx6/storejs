(function(store){

	var mongo = store.Mongodb = store.Initor(),
		ts = require('tsk').ToolStack,
		util = ts.Utility,
		helpers = ts.Helpers.HashMaps,
		Promise = ts.Promise;

	mongo.use = function(){

	};


})(StoreJS);